apply plugin: 'java'
apply plugin: 'jacoco'

version "1.0.1"

repositories {
    jcenter()
}

task wrapper(type: Wrapper) {
    gradleVersion = '5.1'
}

dependencies {
    testCompile 'junit:junit:4.12'
}
